server :
   port: 8081

spring:
   cloud:
      config:
         server:
            git:
               uri: https://github.com/sebrodval/examenGFT.git
               searchPaths: config-data
               
   security:
      user:
         name: root
         password: pass 
  
